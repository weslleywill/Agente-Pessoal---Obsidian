---
phase: 01-funda-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Vault/.gitignore
  - Vault/.gitkeep
  - Vault/00 - Dashboard/.gitkeep
  - Vault/01 - Diário/.gitkeep
  - Vault/02 - Semanal/.gitkeep
  - Vault/03 - Mensal/.gitkeep
  - Vault/04 - Benchmarks/.gitkeep
  - Vault/05 - Rotinas/.gitkeep
  - Vault/06 - Metas/.gitkeep
  - Vault/07 - Frases/.gitkeep
  - Vault/08 - Livros/.gitkeep
  - Vault/09 - Pessoas/.gitkeep
  - Vault/10 - Projetos/.gitkeep
  - Vault/Templates/.gitkeep
  - Vault/_meta/README.md
  - Vault/_meta/plugins.md
  - Vault/_meta/setup.md
  - Vault/_meta/backup.md
  - Vault/_private/.gitkeep
autonomous: true
requirements:
  - FOUND-01
  - FOUND-05
  - FOUND-07
  - FOUND-08
tags:
  - obsidian
  - vault-skeleton
  - windows

must_haves:
  truths:
    - "Usuário abre Obsidian em Vault/ e vê 13 pastas de topo (00-Dashboard até 10-Projetos + Templates + _meta + _private)"
    - "Arquivo .gitignore existe e exclui workspace.json + _private/ + .obsidian/cache"
    - "_meta/plugins.md documenta os 10 plugins com versão atual + status manutenção + fallback"
    - "_meta/setup.md contém instruções passo-a-passo para Weslley configurar Windows + GitHub + plugins"
    - "_meta/backup.md documenta estratégia obsidian-git → GitHub privado com intervalo 30min"
  artifacts:
    - path: "Vault/.gitignore"
      provides: "Git exclusion rules (workspace.json, _private/, .obsidian/cache, .trash/)"
      contains: "workspace.json"
    - path: "Vault/_meta/plugins.md"
      provides: "Plugin registry com versão + status + fallback por plugin"
      min_lines: 50
    - path: "Vault/_meta/setup.md"
      provides: "Step-by-step setup guide (Registry, GitHub, plugins)"
      min_lines: 40
    - path: "Vault/_meta/backup.md"
      provides: "Backup strategy documentation (git-based)"
      min_lines: 20
    - path: "Vault/_meta/README.md"
      provides: "Index do _meta/ explicando propósito de cada arquivo"
    - path: "Vault/00 - Dashboard/.gitkeep"
      provides: "Empty folder placeholder (force git to track dir)"
  key_links:
    - from: "Vault/_meta/plugins.md"
      to: "FOUND-05 requirement"
      via: "Documentação de cada plugin com fallback"
      pattern: "obsidian-git|Templater|Dataview|Periodic Notes|Calendar|Tasks|Homepage|Tracker|Pomodoro|Natural Language"
    - from: "Vault/.gitignore"
      to: "FOUND-07 requirement"
      via: "3 padrões obrigatórios"
      pattern: "workspace.json|_private/|.obsidian/cache"
    - from: "Vault/ structure"
      to: "FOUND-08 requirement"
      via: "13 top-level folders + .gitkeep em cada"
      pattern: "00 - Dashboard|01 - Diário|Templates|_meta|_private"
---

<objective>
Criar skeleton completo do Vault Obsidian: 13 pastas de topo + `.gitignore` + documentação `_meta/`. Tudo isso sem envolver ação manual do Weslley — Claude cria toda a estrutura de arquivos e pastas para que Weslley só precise abrir o Obsidian depois.

**Purpose:** Sem skeleton de folders, nenhum outro plano funciona (Plan 02 precisa de pasta pra git init; Plan 03 precisa de Vault pra apontar o Obsidian). Esta é a base autonoma de Phase 1.

**Output:** Vault/ completo com 13 folders + 4 arquivos de documentação em _meta/ + .gitignore congelado. Weslley ao final pode abrir Obsidian apontando pra Vault/ e ver a estrutura carregando sem erros.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Estrutura de folders canônica do projeto (derivada de research ARCHITECTURE + PROJECT.md) -->
<!-- Todos os nomes em PT-BR com espaço " - " separador (padrão Joel Jota planner) -->

13 pastas de topo (ordem numérica + Templates + _meta + _private):

```
Vault/
├── 00 - Dashboard/        # Home.md, Semana-Atual.md, Mes-Atual.md (Phase 3)
├── 01 - Diário/           # YYYY/MM-MMMM/YYYY-MM-DD.md (single source of truth)
├── 02 - Semanal/          # GGGG/GGGG-[W]WW.md (ISO week, Dataview agg)
├── 03 - Mensal/           # YYYY/YYYY-MM.md (Dataview agg)
├── 04 - Benchmarks/       # Arquivo/ frozen snapshots + PDF/ exports
├── 05 - Rotinas/          # Rotina-Dia-Util.md + Ativa.md (ponteiro)
├── 06 - Metas/            # Metas 6m/1a + _INDEX.md
├── 07 - Frases/           # Banco-Frases.md (50 seed)
├── 08 - Livros/           # Anti-hallucination layer: 1 nota por livro + _INDEX.md
├── 09 - Pessoas/          # Extra-F dashboard (Phase 6)
├── 10 - Projetos/         # Projetos ativos
├── Templates/             # Templater source (incluindo _fragments/ e _config/)
├── _meta/                 # Project metadata (NÃO vault conteúdo): schema, rubric, plugins, setup, backup
└── _private/              # Reflexões sensíveis; EXCLUÍDO do git backup (privacy pitfall #14)
```

Plugins a documentar em `_meta/plugins.md` (10 canônicos — verificado em STACK.md 2026-04-23):

| # | Plugin | Community ID | Versão | Status | Fallback |
|---|--------|--------------|--------|--------|----------|
| 1 | Templater | templater-obsidian | 2.19.3 | ATIVO | — (core Templates é muito primitivo) |
| 2 | Dataview | dataview | 0.5.70 | MAINTENANCE-ONLY | Bases core (futuro, Phase 6+) |
| 3 | Periodic Notes | periodic-notes | 1.0.0-beta.3 | ABANDONADO (funcional) | Periodic Notes Calendar (luiisca) |
| 4 | Calendar | calendar | 2.0.0-beta.2 | ABANDONADO (funcional) | Periodic Notes Calendar (luiisca) |
| 5 | Tasks | obsidian-tasks-plugin | 7.23.1 | ATIVO | Task Genius / TaskForge (less mature) |
| 6 | Homepage | homepage | 4.4.0 | ATIVO | — (único na função) |
| 7 | Tracker | obsidian-tracker | 1.19.0 | ATIVO | Charts (descartado — 2 anos sem release) |
| 8 | Pomodoro Timer | pomodoro-timer | 1.2.3 | LIMBO | TaskNotes (refatoração grande) |
| 9 | Natural Language Dates | nldates-obsidian | 0.6.2 | ABANDONADO (funcional) | nldates-revived (drop-in) |
| 10 | obsidian-git | obsidian-git | Vinzent03 | ATIVO | — (backup crítico) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar skeleton de 13 pastas do Vault com .gitkeep</name>
  <read_first>
    - E:\Claude Code\Agente Pessoal - Obsidian\.planning\PROJECT.md (para confirmar Vault path)
    - E:\Claude Code\Agente Pessoal - Obsidian\.planning\research\SUMMARY.md (para confirmar 13 folders canônicos)
    - Verificar se Vault/ já existe: `ls -la "E:/Claude Code/Agente Pessoal - Obsidian/" | grep Vault`
  </read_first>
  <files>
    Vault/.gitkeep
    Vault/00 - Dashboard/.gitkeep
    Vault/01 - Diário/.gitkeep
    Vault/02 - Semanal/.gitkeep
    Vault/03 - Mensal/.gitkeep
    Vault/04 - Benchmarks/.gitkeep
    Vault/05 - Rotinas/.gitkeep
    Vault/06 - Metas/.gitkeep
    Vault/07 - Frases/.gitkeep
    Vault/08 - Livros/.gitkeep
    Vault/09 - Pessoas/.gitkeep
    Vault/10 - Projetos/.gitkeep
    Vault/Templates/.gitkeep
    Vault/_private/.gitkeep
  </files>
  <action>
    Criar o subfolder `Vault/` dentro de `E:\Claude Code\Agente Pessoal - Obsidian\` e dentro dele criar 13 folders com nomes EXATOS (espaço " - " separador, PT-BR, sem acentos no filename pra evitar Windows MAX_PATH edge cases em nomes futuros):

    1. `Vault/00 - Dashboard/`
    2. `Vault/01 - Diário/`
    3. `Vault/02 - Semanal/`
    4. `Vault/03 - Mensal/`
    5. `Vault/04 - Benchmarks/`
    6. `Vault/05 - Rotinas/`
    7. `Vault/06 - Metas/`
    8. `Vault/07 - Frases/`
    9. `Vault/08 - Livros/`
    10. `Vault/09 - Pessoas/`
    11. `Vault/10 - Projetos/`
    12. `Vault/Templates/`
    13. `Vault/_private/`

    Além disso: `Vault/_meta/` (criado na Task 2, mas o folder precisa existir aqui).

    Dentro de CADA folder colocar um `.gitkeep` vazio (força git a trackear folders vazios — sem isso, `git init` em Plan 02 vai ignorar os folders).

    Comandos bash (executar do diretório raiz do projeto — forward slashes no bash mesmo em Windows):
    ```bash
    cd "/e/Claude Code/Agente Pessoal - Obsidian"
    mkdir -p "Vault/00 - Dashboard" "Vault/01 - Diário" "Vault/02 - Semanal" "Vault/03 - Mensal" "Vault/04 - Benchmarks" "Vault/05 - Rotinas" "Vault/06 - Metas" "Vault/07 - Frases" "Vault/08 - Livros" "Vault/09 - Pessoas" "Vault/10 - Projetos" "Vault/Templates" "Vault/_meta" "Vault/_private"
    touch "Vault/00 - Dashboard/.gitkeep" "Vault/01 - Diário/.gitkeep" "Vault/02 - Semanal/.gitkeep" "Vault/03 - Mensal/.gitkeep" "Vault/04 - Benchmarks/.gitkeep" "Vault/05 - Rotinas/.gitkeep" "Vault/06 - Metas/.gitkeep" "Vault/07 - Frases/.gitkeep" "Vault/08 - Livros/.gitkeep" "Vault/09 - Pessoas/.gitkeep" "Vault/10 - Projetos/.gitkeep" "Vault/Templates/.gitkeep" "Vault/_private/.gitkeep"
    ```

    **Por que `.gitkeep` e não `README.md`:** pitfall #14 (privacy leak) + pitfall #11 (MAX_PATH). Folders vazios só existem depois de git init se tiverem placeholder. `.gitkeep` é convenção universal (zero bytes, zero ambiguidade).

    **Por que não criar `_meta/.gitkeep`:** Task 2 cria arquivos reais em `_meta/` (README.md, plugins.md, etc.), então o folder é rastreado automaticamente por esses arquivos.
  </action>
  <verify>
    <automated>
      cd "/e/Claude Code/Agente Pessoal - Obsidian" && test -d "Vault/00 - Dashboard" && test -d "Vault/01 - Diário" && test -d "Vault/02 - Semanal" && test -d "Vault/03 - Mensal" && test -d "Vault/04 - Benchmarks" && test -d "Vault/05 - Rotinas" && test -d "Vault/06 - Metas" && test -d "Vault/07 - Frases" && test -d "Vault/08 - Livros" && test -d "Vault/09 - Pessoas" && test -d "Vault/10 - Projetos" && test -d "Vault/Templates" && test -d "Vault/_meta" && test -d "Vault/_private" && test -f "Vault/00 - Dashboard/.gitkeep" && test -f "Vault/_private/.gitkeep" && echo "PASS: 14 folders + gitkeeps OK" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls "Vault/"` retorna exatamente 14 entradas (13 folders numerados/nomeados + `_meta/`)
    - `ls "Vault/00 - Dashboard/"` retorna `.gitkeep` (folder não-vazio do ponto de vista git)
    - Todos os 13 folders não-`_meta` contêm exatamente 1 arquivo: `.gitkeep`
    - `_meta/` existe como folder (arquivos virão na Task 2)
    - `find Vault -maxdepth 2 -name .gitkeep | wc -l` retorna >= 13
  </acceptance_criteria>
  <done>
    Rodando `ls -la Vault/` retorna 14 folders visíveis (sem arquivos soltos) e `find Vault -name .gitkeep` retorna >= 13 paths. Executor pode abrir Obsidian apontando pra `Vault/` sem erro de carregamento.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar _meta/ documentation (README, plugins, setup, backup)</name>
  <read_first>
    - E:\Claude Code\Agente Pessoal - Obsidian\.planning\research\STACK.md (linhas 11-22 para tabela de plugins + versões)
    - E:\Claude Code\Agente Pessoal - Obsidian\.planning\research\PITFALLS.md (pitfall #3 plugin abandonment, #5 sync corruption, #11 MAX_PATH)
    - Confirmar que Vault/_meta/ existe: `ls -la "Vault/_meta/"`
  </read_first>
  <files>
    Vault/_meta/README.md
    Vault/_meta/plugins.md
    Vault/_meta/setup.md
    Vault/_meta/backup.md
  </files>
  <action>
    Criar 4 arquivos de documentação em `Vault/_meta/`. Todos em PT-BR. Conteúdo EXATO abaixo — copiar, não improvisar.

    ---

    **Arquivo 1: `Vault/_meta/README.md`** (índice do folder)

    ```markdown
    # _meta — Metadata do Projeto (não é conteúdo do vault)

    Este folder contém documentação OPERACIONAL do projeto Agente Pessoal Obsidian. Diferente de `00 - Dashboard/` ... `10 - Projetos/` (que são o conteúdo do planner), `_meta/` documenta COMO o vault é construído.

    ## Arquivos

    | Arquivo | O que é |
    |---|---|
    | `README.md` | Este índice |
    | `plugins.md` | Registro dos 10 plugins community + versão + status manutenção + fallback se quebrar |
    | `setup.md` | Guia passo-a-passo de setup (Windows Registry, GitHub, instalação de plugins) |
    | `backup.md` | Estratégia de backup (obsidian-git → GitHub privado, intervalo 30min, anti-sync-corruption) |

    ## Arquivos futuros (Phase 2+)

    | Arquivo | Phase | O que é |
    |---|---|---|
    | `schema.md` | Phase 2 | Schema frontmatter congelado (snake_case PT-BR) |
    | `rubric.md` | Phase 2 | Âncoras concretas 1-5 por pilar |
    | `fields.md` | Phase 2 | Justificativa de cada campo no template |
    | `calibration.md` | Phase 4 | Histórico mediana tempo_real por tipo de tarefa (coach feedback loop) |
    | `migrations/` | Phase 2+ | Templater scripts de migração se schema mudar |

    ## Regra

    `_meta/` NÃO contém reflexão pessoal. Só documentação operacional. Tudo aqui é versionado no git (diferente de `_private/` que fica fora do backup).
    ```

    ---

    **Arquivo 2: `Vault/_meta/plugins.md`** (registro dos 10 plugins)

    ```markdown
    # Plugins — Registro e Fallbacks

    **Atualizado:** 2026-04-23 (verificado via GitHub releases na research/STACK.md)

    10 plugins community obrigatórios para v1. 4 estão em status stale/limbo mas funcionais. TODOS têm rota de fallback documentada (pitfall #3: plugin abandonment).

    **Endereça:** FOUND-05

    ---

    ## Tabela de plugins

    | # | Plugin | Community ID | Versão alvo | Status manutenção | O que quebra se morrer | Fallback |
    |---|--------|--------------|-------------|-------------------|------------------------|----------|
    | 1 | Templater | `templater-obsidian` | 2.19.3 | ATIVO (release 2026-04-22) | Folder-template mapping, lógica condicional, user functions. Daily note vira só Daily Notes core. | Core "Templates" (primitivo; sem folder mapping; aceitar degradação) |
    | 2 | Dataview | `dataview` | 0.5.70 | MAINTENANCE-ONLY (autor foco em Datacore) | Todas queries do dashboard, weekly/monthly agregações, rollups | Bases core plugin (Obsidian 1.9+); migração parcial em Phase 6+ |
    | 3 | Periodic Notes | `periodic-notes` | 1.0.0-beta.3 | ABANDONADO (funcional) | Weekly/Monthly/Quarterly/Yearly criação estruturada | Periodic Notes Calendar (luiisca) — drop-in mas precisa re-configurar |
    | 4 | Calendar | `calendar` | 2.0.0-beta.2 | ABANDONADO (funcional; 5 anos sem update) | Widget sidebar clicável; integração com Periodic Notes | QuickCalendar (snovis) ou Periodic Notes Calendar (luiisca) |
    | 5 | Tasks | `obsidian-tasks-plugin` | 7.23.1 | ATIVO | Queries de tarefas pendentes, recurring (🔁 every week), syntax emojis | Task Genius / TaskForge (menos maduros; refatoração grande) |
    | 6 | Homepage | `homepage` | 4.4.0 | ATIVO | Abertura automática de `00 - Dashboard/Home.md` em Reading mode | Nenhum — abrir manualmente (degradação aceitável) |
    | 7 | Tracker | `obsidian-tracker` | 1.19.0 | ATIVO (commit <10 dias) | Charts dos 5 pilares ao longo do tempo em Semana-Atual/Mes-Atual | Charts plugin DESCARTADO (2 anos sem release). Life Tracker (Bases-native) como alternativa futura |
    | 8 | Pomodoro Timer | `pomodoro-timer` | 1.2.3 | LIMBO (último push 2024-06) | Pomodoros ligados a tasks específicas; `pomodoros::N` inline field | TaskNotes (callumalpass) — reescreve tasks como notas (refatoração grande; só se Pomodoro quebrar) |
    | 9 | Natural Language Dates | `nldates-obsidian` | 0.6.2 | ABANDONADO (funcional) | Input `@tomorrow`, `@next friday` em Tasks e daily notes | nldates-revived (Amato21) — drop-in, zero refactor |
    | 10 | obsidian-git | `obsidian-git` | Latest (Vinzent03, ativo) | ATIVO | Backup automático 30min → GitHub privado | git CLI manual (`git commit -am` + `git push`) — manual mas possível |

    ---

    ## Ordem de instalação recomendada

    1. Templater (base para criação de notas)
    2. Periodic Notes (estrutura daily/weekly/monthly)
    3. Calendar (widget sidebar)
    4. Natural Language Dates (input dates)
    5. Tasks
    6. Dataview
    7. Homepage
    8. Tracker
    9. Pomodoro Timer
    10. obsidian-git

    ---

    ## Conflitos conhecidos

    - **Tasks ↔ Pomodoro Timer**: `pomodoros::N` inline field DEVE vir ANTES dos emojis Tasks (`📅 ⏫`) na linha. Caso contrário, Tasks plugin não parseia o resto.
    - **Periodic Notes + core Daily Notes**: precisam estar sincronizados (mesmo folder, mesmo template) — ou desabilitar core Daily Notes.
    - **Homepage ↔ Dataview**: issue antigo (#79 com Obsidian 1.5.6) já resolvido em Homepage 4.4.0.

    ---

    ## Auditoria periódica

    Re-verificar este arquivo em cada milestone boundary:
    - Plugin teve commit nos últimos 90 dias? (se não → flag monitoramento)
    - Plugin ainda funciona na versão atual do Obsidian? (testar)
    - Fallback ainda é válido? (fallback pode ter abandonado também)

    Pendência Phase 1: validar decisão final Pomodoro Timer vs TaskNotes durante audit da Phase 1 (ver STATE.md).
    ```

    ---

    **Arquivo 3: `Vault/_meta/setup.md`** (guia step-by-step para Weslley)

    ```markdown
    # Setup Guide — Fase 1 Foundation

    Passos manuais que Weslley precisa executar para completar Fase 1. Claude fez a parte autonoma (skeleton de folders + documentação); este guia cobre o resto.

    **Endereça:** FOUND-02 (Windows LongPaths), FOUND-03 (10 plugins), FOUND-04 (Templater config), FOUND-06 (GitHub privado)

    ---

    ## 1. Windows LongPaths (FOUND-02) — resolve pitfall #11 (MAX_PATH 260)

    **Por quê:** Windows 11 ainda usa MAX_PATH=260 por padrão. Vault em `E:\Claude Code\Agente Pessoal - Obsidian\Vault\...` + nota com nome longo + subfolder + anexo = path >260 → git falha silenciosamente, File Explorer não abre o arquivo, backup tools pulam.

    **Como fazer:**

    Opção A — Registry (mais rápido, requer admin):
    1. Abra PowerShell como Administrador
    2. Execute:
       ```powershell
       reg add "HKLM\SYSTEM\CurrentControlSet\Control\FileSystem" /v LongPathsEnabled /t REG_DWORD /d 1 /f
       ```
    3. Reinicie o computador (mudança só aplica após reboot)

    Opção B — Group Policy (interface gráfica):
    1. `Win+R` → `gpedit.msc`
    2. Navegue: Local Computer Policy → Computer Configuration → Administrative Templates → System → Filesystem
    3. Abra **"Enable Win32 long paths"** → Enabled → OK
    4. Reinicie

    **Verificação:** Após reboot, rode em PowerShell:
    ```powershell
    Get-ItemPropertyValue -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name LongPathsEnabled
    ```
    Deve retornar `1`.

    ---

    ## 2. GitHub Private Repo + obsidian-git (FOUND-06) — resolve pitfall #14 (privacy leak)

    **Por quê:** Sem backup local only = 1 disco morre = tudo perdido. MAS GitHub repo público = 6+ meses de reflexões pessoais world-readable. Regra: repo é **PRIVADO**, sem exceção.

    **Como fazer:**

    1. Entre em https://github.com/new
    2. Preencha:
       - **Repository name:** `agente-pessoal-obsidian-vault` (ou nome de sua escolha)
       - **Description:** "Vault pessoal Obsidian (Agente Pessoal) — privado"
       - **Visibility:** ✅ **Private** (DOUBLE-CHECK! botão tem que estar marcado em Private, NUNCA Public)
       - **Initialize repository:** NÃO marcar nenhum (README/license/gitignore) — vai conflitar com init local
    3. Clique "Create repository"
    4. Copie a URL `git@github.com:SEUUSUARIO/agente-pessoal-obsidian-vault.git` (SSH) OU `https://github.com/SEUUSUARIO/agente-pessoal-obsidian-vault.git` (HTTPS)

    **Verificação dupla de privacidade:**
    - Abra a URL do repo no browser
    - Cabeçalho deve mostrar 🔒 + "Private" ao lado do nome
    - Se vir "Public" → deletar e recriar (não dá pra arriscar)

    Ver Plan 02 para `git init` + primeiro push.

    ---

    ## 3. Instalação dos 10 Plugins (FOUND-03) + Configuração Templater (FOUND-04)

    Ver `_meta/plugins.md` para lista completa. Ordem de instalação recomendada lá.

    **Procedimento para cada plugin:**

    1. Abrir Obsidian apontando para `E:\Claude Code\Agente Pessoal - Obsidian\Vault\`
    2. `Settings` (⚙) → `Community plugins` → `Turn on community plugins` (se primeira vez)
    3. `Browse` → buscar pelo Community ID (ex: `templater-obsidian`)
    4. `Install` → `Enable`
    5. Ir em `Settings` → `Community plugins` e confirmar que aparece "Enabled"

    **Configuração específica Templater (FOUND-04):**

    1. `Settings` → `Templater`
    2. **Template folder location:** `Templates/` (exatamente com a barra)
    3. **Trigger Templater on new file creation:** ✅ **ON** (crítico — sem isso Templater não dispara automaticamente)
    4. **Folder Template Settings:** (configurar em Phase 2 quando os templates existirem — por enquanto deixar vazio)

    **Configuração Homepage:**

    1. `Settings` → `Homepage`
    2. **Homepage:** `00 - Dashboard/Home.md` (vai estar vazio até Phase 3, mas aponta)
    3. **Open on startup:** ✅ ON
    4. **Open in Reading view:** ✅ ON (importante — Dataview só renderiza em Reading)

    Ver Plan 03 para lista exaustiva de configurações de cada plugin.

    ---

    ## 4. Checklist final Phase 1

    Marcar cada item quando completado:

    - [ ] Windows `LongPathsEnabled=1` (verificado via PowerShell)
    - [ ] GitHub repo criado e confirmado **Private** 🔒
    - [ ] `git init` + remote + primeiro push feito (Plan 02)
    - [ ] 10 plugins instalados e habilitados (Plan 03)
    - [ ] Templater "Trigger on new file = ON" + folder `Templates/`
    - [ ] Homepage apontando pra `00 - Dashboard/Home.md` em Reading mode
    - [ ] obsidian-git configurado com auto-commit 30min
    - [ ] Teste: criar nota com path >250 chars em `01 - Diário/` → abre sem erro
    - [ ] Teste: `Ctrl+N` cria nota em folder mapeado e Templater dispara (após Phase 2 que cria templates)
    ```

    ---

    **Arquivo 4: `Vault/_meta/backup.md`** (estratégia de backup)

    ```markdown
    # Backup Strategy

    **Endereça:** FOUND-06, FOUND-07. Resolve pitfalls #5 (sync corruption) e #14 (privacy leak).

    ---

    ## Regra #1: NUNCA em cloud sync

    Vault **JAMAIS** pode ficar em:
    - ❌ OneDrive (qualquer folder sincronizado)
    - ❌ iCloud Drive (Windows é especialmente bugado — duplica até 56x)
    - ❌ Dropbox
    - ❌ Google Drive folder sincronizado

    **Path atual correto:** `E:\Claude Code\Agente Pessoal - Obsidian\Vault\` (drive E: local, não sincronizado) ✓

    **Se acontecer corrupção (arquivos `(1).md`, `(2).md`, hostname-suffix):**
    - Fechar Obsidian
    - Fechar cliente de sync (se houver)
    - `git reset --hard HEAD` para voltar ao último commit limpo
    - Mover vault para fora do folder sync
    - Documentar o incidente em `_meta/incidents.md`

    ---

    ## Regra #2: Backup único é obsidian-git → GitHub privado

    - **Plugin:** obsidian-git (Vinzent03)
    - **Repo:** GitHub, **PRIVATE** (verificação dupla — ver `setup.md` §2)
    - **Intervalo auto-commit:** a cada 30 minutos de atividade + ao fechar Obsidian
    - **Commit message format:** `vault: YYYY-MM-DD HH:MM` (obsidian-git default ok)

    **Configurar em Obsidian:**
    1. `Settings` → `Obsidian Git`
    2. **Vault backup interval (minutes):** `30`
    3. **Commit when Obsidian is closing:** ✅ ON
    4. **Auto pull on startup:** ✅ ON (single-device, mas por segurança)
    5. **Commit message:** deixar default ou `vault: {{date}} {{hostname}}`

    ---

    ## Regra #3: .gitignore é sagrado

    Arquivo `Vault/.gitignore` (criado por Plan 01 Task 3) exclui:
    - `workspace.json` (muda a cada abertura — git noise)
    - `.obsidian/cache/` (cache local, não faz sentido versionar)
    - `_private/` (reflexões sensíveis — privacy pitfall #14)
    - `.obsidian/plugins/*/data.json` (dados específicos de instalação)
    - `.trash/` (Obsidian built-in trash)

    **Se mudar algo aqui → revisar todos os commits passados pra checar se nada sensível escapou.**

    ---

    ## Regra #4: Verificação mensal

    Todo final de mês:
    1. Abrir GitHub repo no browser
    2. Verificar badge: 🔒 Private (se vier Public → emergência, rotacionar tudo sensível que possa ter vazado)
    3. Conferir último commit: deve ter timestamp do mês atual
    4. Se >7 dias sem commit: obsidian-git pode ter quebrado; verificar plugin

    ---

    ## Recovery

    | Cenário | Ação |
    |---|---|
    | Perdi último dia de trabalho | `git log --oneline` + `git checkout <commit>` do arquivo específico |
    | Corrupção por sync (arquivos `(N).md`) | `git reset --hard HEAD` + mover vault pra fora de pasta sync |
    | Plugin obsidian-git quebrou | Fallback: `git commit -am "manual: YYYY-MM-DD"` + `git push` via terminal |
    | HD morreu | `git clone` do remote privado → vault reconstituído |
    | Accidentally committed `_private/` | `git filter-branch` ou `bfg-repo-cleaner` + force-push + rotate qualquer credencial mencionada |
    ```

    ---

    **Por que esses 4 arquivos e não outros:**
    - `README.md`: navigability
    - `plugins.md`: FOUND-05 canônico
    - `setup.md`: FOUND-02/03/04/06 passo-a-passo (consumido por Plans 02 e 03)
    - `backup.md`: aprofunda FOUND-06/07 e referencia pitfalls

    **Não criar agora:** `schema.md`, `rubric.md`, `fields.md`, `calibration.md`, `incidents.md`. Todos são Phase 2+ (ver `README.md` tabela "Arquivos futuros").
  </action>
  <verify>
    <automated>
      cd "/e/Claude Code/Agente Pessoal - Obsidian" && test -f "Vault/_meta/README.md" && test -f "Vault/_meta/plugins.md" && test -f "Vault/_meta/setup.md" && test -f "Vault/_meta/backup.md" && grep -q "obsidian-git" "Vault/_meta/plugins.md" && grep -q "Templater" "Vault/_meta/plugins.md" && grep -q "Dataview" "Vault/_meta/plugins.md" && grep -q "LongPathsEnabled" "Vault/_meta/setup.md" && grep -q "Private" "Vault/_meta/setup.md" && grep -q "OneDrive" "Vault/_meta/backup.md" && echo "PASS: 4 files + key content present" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `Vault/_meta/README.md` existe, > 20 linhas, menciona `plugins.md`, `setup.md`, `backup.md`
    - `Vault/_meta/plugins.md` existe, > 50 linhas, contém tabela com exatamente 10 plugins (Templater, Dataview, Periodic Notes, Calendar, Tasks, Homepage, Tracker, Pomodoro Timer, Natural Language Dates, obsidian-git); cada linha tem versão + status + fallback
    - `Vault/_meta/setup.md` existe, > 40 linhas, contém: comando `reg add ... LongPathsEnabled`, URL `https://github.com/new`, string "Private" (para visibilidade), instruções Templater "Trigger Templater on new file creation"
    - `Vault/_meta/backup.md` existe, > 20 linhas, menciona OneDrive/iCloud/Dropbox proibidos, obsidian-git 30min, `.gitignore` rules
    - Todos os 4 arquivos são válidos markdown (grep não retorna erro de encoding)
    - grep `Templater` em `plugins.md` retorna ≥1 match; grep `obsidian-git` retorna ≥1 match
  </acceptance_criteria>
  <done>
    Pasta `Vault/_meta/` contém os 4 arquivos. Executor pode ler `plugins.md` e tem em uma tabela TUDO que precisa para Plan 03 (versões, IDs, status, fallbacks). Weslley pode ler `setup.md` e ter guia completo do que fazer manualmente.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar .gitignore do Vault (FOUND-07)</name>
  <read_first>
    - E:\Claude Code\Agente Pessoal - Obsidian\.planning\research\PITFALLS.md (pitfall #14 privacy leak — contexto de `_private/` no gitignore)
    - E:\Claude Code\Agente Pessoal - Obsidian\.planning\REQUIREMENTS.md FOUND-07 (3 padrões obrigatórios)
    - Verificar que Vault/ existe: `ls -la Vault/`
  </read_first>
  <files>
    Vault/.gitignore
  </files>
  <action>
    Criar `Vault/.gitignore` com conteúdo EXATO abaixo. NÃO improvisar padrões — cada linha tem razão documentada em PITFALLS.md.

    Conteúdo literal do arquivo:

    ```
    # Vault .gitignore — Agente Pessoal Obsidian
    # Gerado em Phase 1 Plan 01. Alterações aqui afetam backup privado.
    # Endereça: FOUND-07. Resolve pitfall #14 (privacy leak).

    # === Obsidian workspace state (muda a cada abertura = git noise) ===
    .obsidian/workspace.json
    .obsidian/workspace-mobile.json
    .obsidian/workspaces.json

    # === Obsidian cache (dados derivados, não-fonte) ===
    .obsidian/cache
    .obsidian/cache/
    .obsidian/plugins/*/data.json.backup

    # === Privacy zone — reflexões sensíveis NUNCA no backup remoto ===
    # Pitfall #14: uma vez pushed, assumir vazado. Rotate credenciais citadas se escapou.
    _private/
    _private/**

    # === Obsidian trash (built-in) ===
    .trash/

    # === Sistema operacional ===
    .DS_Store
    Thumbs.db
    desktop.ini

    # === Editor state ===
    .vscode/
    .idea/

    # === Templater temporários ===
    Templates/_fragments/.cache/
    ```

    **Justificativa por bloco:**
    - `workspace.json`: Obsidian reescreve a cada foco/fechamento de painel. Commit a cada 30min vira dump inútil de layout. Exclui.
    - `.obsidian/cache`: resultado de parse de frontmatter/links, reconstruído ao abrir. Não-fonte.
    - `_private/`: REGRA DE OURO. Este folder existe para o Coach ler localmente mas NUNCA vazar. Se aparecer em `git status` → bug crítico.
    - `.trash/`: items deletados via Obsidian UI. Sobrepor ao remote vai contra workflow.
    - OS files: ruído multi-plataforma.
    - `Templates/_fragments/.cache/`: pre-emptive — Templater pode cachear aqui em Phase 2.

    **NÃO gitignorar (ainda que pareça candidato):**
    - `.obsidian/community-plugins.json`: queremos no backup (lista de quais plugins estão habilitados — importante pra restore em máquina nova)
    - `.obsidian/appearance.json`: preferências de tema, OK versionar
    - `.obsidian/hotkeys.json`: atalhos custom, versionar

    Comando bash para criar o arquivo (usando cat com heredoc é OK aqui por ser só criação inicial; se o arquivo já existir, Read primeiro):

    ```bash
    # Executor usa a Write tool, não heredoc. O bloco acima é o conteúdo literal.
    ```
  </action>
  <verify>
    <automated>
      cd "/e/Claude Code/Agente Pessoal - Obsidian" && test -f "Vault/.gitignore" && grep -q "^workspace.json" "Vault/.gitignore" -F 2>/dev/null || grep -q "workspace.json" "Vault/.gitignore" && grep -q "_private/" "Vault/.gitignore" && grep -q ".obsidian/cache" "Vault/.gitignore" && grep -q "FOUND-07" "Vault/.gitignore" && echo "PASS: gitignore has all 3 mandatory patterns + docs" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `Vault/.gitignore` existe e tem > 20 linhas
    - `grep "workspace.json" Vault/.gitignore` retorna match (FOUND-07 requisito 1)
    - `grep "_private/" Vault/.gitignore` retorna match (FOUND-07 requisito 2)
    - `grep ".obsidian/cache" Vault/.gitignore` retorna match (FOUND-07 requisito 3)
    - Arquivo começa com comentário `# Vault .gitignore` (self-documenting)
    - Arquivo menciona pitfall #14 (documenta WHY, não só WHAT)
  </acceptance_criteria>
  <done>
    `Vault/.gitignore` existe com os 3 padrões obrigatórios do FOUND-07 + comentários explicativos. Plan 02 pode rodar `git init` + `git add .` sem medo de capturar `_private/` ou `workspace.json`.
  </done>
</task>

</tasks>

<verification>
Verificação do plano completo (rodar todas as 3 tasks e depois):

1. **Estrutura de folders:**
   ```bash
   find Vault -maxdepth 2 -type d | sort
   ```
   Deve listar Vault/ + 14 subfolders (13 numerados/nomeados + _meta).

2. **.gitkeep presente em folders vazios:**
   ```bash
   find Vault -maxdepth 2 -name ".gitkeep" | wc -l
   ```
   Deve retornar ≥ 13.

3. **Documentação _meta/ completa:**
   ```bash
   ls Vault/_meta/ | sort
   ```
   Deve listar: README.md, backup.md, plugins.md, setup.md (4 arquivos).

4. **.gitignore com padrões obrigatórios:**
   ```bash
   grep -E "(workspace.json|_private/|.obsidian/cache)" Vault/.gitignore | wc -l
   ```
   Deve retornar ≥ 3.

5. **Sanity check de conteúdo:**
   - `plugins.md` menciona os 10 plugins canônicos por nome
   - `setup.md` menciona `LongPathsEnabled` e `Private`
   - `backup.md` menciona `OneDrive` (no bloco "proibido")

6. **Abrir Obsidian apontando pra Vault/:**
   - File → Open folder as vault → `E:\Claude Code\Agente Pessoal - Obsidian\Vault\`
   - Deve carregar sem erro, mostrando os 13 folders + _meta no left sidebar
   - (Esta é verificação manual; acceptance OK se folders existem em filesystem)
</verification>

<success_criteria>
Plan 01 completo quando:

- [ ] `Vault/` existe com 14 subfolders (13 numerados/nomeados + _meta)
- [ ] Cada folder exceto _meta tem `.gitkeep` (git rastreará todos)
- [ ] `Vault/_meta/README.md` existe (index do folder)
- [ ] `Vault/_meta/plugins.md` existe documentando 10 plugins + versão + status + fallback (FOUND-05)
- [ ] `Vault/_meta/setup.md` existe com instruções passo-a-passo para Windows + GitHub + Obsidian plugins
- [ ] `Vault/_meta/backup.md` existe documentando obsidian-git → GitHub privado
- [ ] `Vault/.gitignore` existe com workspace.json + _private/ + .obsidian/cache (FOUND-07)
- [ ] Requisitos endereçados: FOUND-01 (Vault path local), FOUND-05 (plugins documentados), FOUND-07 (gitignore), FOUND-08 (folder skeleton)

**Sinaliza pronto para:** Plan 02 (git init + GitHub + Windows Registry) e Plan 03 (instalação de plugins via Obsidian UI) — ambos podem rodar paralelamente em wave 2.
</success_criteria>

<output>
Após completion, criar `.planning/phases/01-funda-o/01-01-SUMMARY.md` com:
- Artefatos criados (lista de files_modified que realmente existem)
- Verificação dos 4 acceptance criteria principais
- Any deviation from plan (se executor teve que improvisar)
- Pattern learned: "dot-prefixed folders + .gitkeep = 13 folders rastreados por git desde o init"
- Próximo passo: Plan 02 (setup manual Windows + GitHub)
</output>
