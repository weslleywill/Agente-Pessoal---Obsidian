---
phase: 01-funda-o
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Vault/.obsidian/community-plugins.json
  - Vault/.obsidian/plugins/templater-obsidian/data.json
  - Vault/.obsidian/plugins/dataview/data.json
  - Vault/.obsidian/plugins/periodic-notes/data.json
  - Vault/.obsidian/plugins/calendar/data.json
  - Vault/.obsidian/plugins/obsidian-tasks-plugin/data.json
  - Vault/.obsidian/plugins/homepage/data.json
  - Vault/.obsidian/plugins/obsidian-tracker/data.json
  - Vault/.obsidian/plugins/pomodoro-timer/data.json
  - Vault/.obsidian/plugins/nldates-obsidian/data.json
  - Vault/.obsidian/plugins/obsidian-git/data.json
  - Vault/.obsidian/core-plugins.json
autonomous: false
requirements:
  - FOUND-03
  - FOUND-04
tags:
  - obsidian
  - plugins
  - manual

user_setup:
  - service: obsidian-app
    why: "Instalar plugins só possível via Community Plugins UI do Obsidian (não via API externa)"
    account_required: false
    dashboard_config:
      - task: "Abrir Vault/ como vault no Obsidian (File → Open folder as vault)"
        location: "Obsidian desktop app"
      - task: "Settings → Community plugins → Turn on community plugins (primeira vez)"
        location: "Obsidian Settings"
      - task: "Browse → buscar e instalar 10 plugins"
        location: "Community plugins browser"

must_haves:
  truths:
    - "Obsidian abre Vault/ sem erro de carregamento"
    - "10 plugins community estão instalados E habilitados (listados em .obsidian/community-plugins.json)"
    - "Templater tem 'Trigger on new file creation' = ON e folder = Templates/"
    - "Homepage aponta para '00 - Dashboard/Home.md' em Reading mode (mesmo que arquivo não exista ainda — Phase 3 cria)"
    - "obsidian-git plugin conectado ao remote privado criado em Plan 02"
    - "obsidian-git auto-commit interval = 30 min; commit-on-close = ON"
  artifacts:
    - path: "Vault/.obsidian/community-plugins.json"
      provides: "Lista de plugins habilitados"
      contains: "templater-obsidian, dataview, periodic-notes, calendar, obsidian-tasks-plugin, homepage, obsidian-tracker, pomodoro-timer, nldates-obsidian, obsidian-git"
      min_items: 10
    - path: "Vault/.obsidian/plugins/templater-obsidian/data.json"
      provides: "Templater config"
      contains: "trigger_on_file_creation"
    - path: "Vault/.obsidian/plugins/obsidian-git/data.json"
      provides: "obsidian-git config"
      contains: "autoSaveInterval"
    - path: "Vault/.obsidian/plugins/homepage/data.json"
      provides: "Homepage target + mode config"
      contains: "Home"
  key_links:
    - from: "Vault/.obsidian/community-plugins.json"
      to: "FOUND-03 requirement"
      via: "JSON array com 10 plugin IDs"
      pattern: "templater-obsidian.*dataview.*periodic-notes.*calendar.*obsidian-tasks-plugin.*homepage.*obsidian-tracker.*pomodoro-timer.*nldates-obsidian.*obsidian-git"
    - from: "Templater config"
      to: "FOUND-04"
      via: "Settings → Templater → Trigger on new file = ON"
      pattern: "trigger_on_file_creation.*true"
    - from: "obsidian-git config"
      to: "Plan 02 remote URL"
      via: "Plugin usa o .git/config.remote.origin.url existente"
---

<objective>
Instalar e configurar os 10 plugins community obrigatórios no Obsidian apontado para `Vault/`, com configuração crítica inicial (Templater trigger, Homepage target, obsidian-git intervals). Este plano é **manual** — Obsidian não tem API externa para instalação de plugins; requer UI interaction.

**Purpose:** Sem os 10 plugins, nada dos Phases 2-6 funciona. Sem Templater com "Trigger on new file = ON", folder template mapping não dispara (Phase 2 depende). Sem obsidian-git conectado ao remote, backup automático não acontece.

**Output:** Obsidian configurado com 10 plugins habilitados + configurações iniciais corretas. Weslley pode fechar Obsidian e reabrir, todos os plugins persistem.

**Autonomous:** false. Cada plugin exige: abrir Obsidian → Community plugins UI → Browse → Install → Enable → Configure (se tem config inicial). Claude não tem controle do Obsidian.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-funda-o/01-01-PLAN.md
@.planning/phases/01-funda-o/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- 10 plugins canônicos — IDs exatos pra buscar no Community plugins browser -->
<!-- Versões-alvo baseadas em STACK.md (verificado 2026-04-23) -->

| Ordem | Community ID (buscar este) | Nome amigável | Versão alvo |
|-------|----------------------------|---------------|-------------|
| 1 | templater-obsidian | Templater | 2.19.3 |
| 2 | periodic-notes | Periodic Notes | 1.0.0-beta.3 |
| 3 | calendar | Calendar | 2.0.0-beta.2 |
| 4 | nldates-obsidian | Natural Language Dates | 0.6.2 |
| 5 | obsidian-tasks-plugin | Tasks | 7.23.1+ |
| 6 | dataview | Dataview | 0.5.70 |
| 7 | homepage | Homepage | 4.4.0 |
| 8 | obsidian-tracker | Tracker | 1.19.0 |
| 9 | pomodoro-timer | Pomodoro Timer | 1.2.3 |
| 10 | obsidian-git | Git | Latest (Vinzent03) |

**Nota sobre versões:** não é crítico ter versão EXATA (Community plugins sempre instala a mais recente disponível). A tabela em STACK.md documenta o que foi verificado como funcional. Se a atual quebra: downgrade é possível mas raramente necessário.

**Plugins core do Obsidian a VERIFICAR (não instalar, já vem):**
- ✅ Daily Notes (core) — deixar habilitado mas configurado igual ao Periodic Notes para evitar conflito
- ⚠️ Templates (core) — pode DESABILITAR (usamos Templater; core Templates é primitivo e potencialmente conflita)

**Arquivos de config do Obsidian** (gerados automaticamente ao configurar; não editar manualmente):
- `Vault/.obsidian/community-plugins.json` — array de IDs habilitados
- `Vault/.obsidian/plugins/<plugin-id>/data.json` — config de cada plugin
- `Vault/.obsidian/core-plugins.json` — array de core plugins habilitados
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Abrir Vault/ no Obsidian + ativar Community Plugins</name>
  <read_first>
    - Verificar que Plan 01 completou: `ls Vault/` deve mostrar 13 folders + _meta
    - Verificar que Obsidian desktop está instalado; se não: baixar em https://obsidian.md/download
  </read_first>
  <files>
    Vault/.obsidian/ (criado automaticamente pelo Obsidian ao abrir como vault)
    Vault/.obsidian/community-plugins.json (vazio após ativar; populado pelas tasks seguintes)
  </files>
  <what-built>
    Plan 01 criou skeleton. Obsidian precisa abrir o folder como "vault" para criar o `.obsidian/` e registrar estado.
  </what-built>
  <how-to-verify>
    1. Abrir Obsidian desktop
    2. Se tela inicial: clicar **"Open folder as vault"** → selecionar `E:\Claude Code\Agente Pessoal - Obsidian\Vault` → Open
       Se já tem outro vault aberto: `File → Open vault → Open folder as vault` → mesmo path
    3. Obsidian abre. Sidebar esquerda mostra 13 folders (+ `_meta`). OK se não tem arquivos (só `.gitkeep` em cada).
    4. **Ativar Community Plugins** (primeira vez):
       - `Settings` (⚙ no canto inferior esquerdo) → `Community plugins`
       - Clicar **"Turn on community plugins"**
       - Aparece aviso sobre riscos. Confirmar.
    5. Depois disso, aparece botão **"Browse"** — NÃO clicar ainda, vai ser usado nas próximas tasks.

    **Sanity check via filesystem:**
    ```bash
    cd "/e/Claude Code/Agente Pessoal - Obsidian/Vault"
    ls -la .obsidian/
    # Esperado: pasta foi criada, contém workspace.json, app.json, community-plugins.json (vazio ou [])
    cat .obsidian/community-plugins.json
    # Esperado: [] (array vazio — ainda não instalou nada)
    ```

    **Ajustes adicionais (opcional mas recomendado agora):**
    - `Settings` → `Core plugins`
    - **Desabilitar "Templates"** (core plugin — vai conflitar com Templater)
    - Confirmar **"Daily notes"** habilitado (vamos configurá-lo em Phase 2 alinhado com Periodic Notes)
    - Confirmar **"Files"**, **"Editor"**, **"Workspaces"** habilitados (defaults)
  </how-to-verify>
  <acceptance_criteria>
    - Vault/.obsidian/ existe e contém `community-plugins.json` (ainda que vazio)
    - Obsidian UI mostra os 13 folders + _meta no left sidebar
    - `Settings → Community plugins` mostra "Restricted mode: OFF" (ou equivalente, dependendo da versão)
    - Botão "Browse" visível e clicável
    - (Opcional) Core plugin "Templates" desabilitado para evitar conflito com Templater
  </acceptance_criteria>
  <resume-signal>
    Digite "Vault aberto + Community ON" quando Obsidian mostrar os folders e Community plugins estiver ativado. Se deu erro abrindo o vault, descreva.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Instalar e habilitar os 10 plugins community</name>
  <read_first>
    - Vault/_meta/plugins.md (tabela com IDs + versões alvo)
    - Vault/_meta/setup.md §3 (procedimento padrão de instalação)
  </read_first>
  <files>
    Vault/.obsidian/community-plugins.json (crescerá a cada install)
    Vault/.obsidian/plugins/<plugin-id>/ (uma pasta por plugin)
  </files>
  <what-built>
    Ambiente Obsidian está pronto (Task 1). Agora instalar cada um dos 10 plugins obrigatórios.
  </what-built>
  <how-to-verify>
    **Para cada um dos 10 plugins abaixo, seguir o procedimento:**

    1. `Settings` → `Community plugins` → `Browse`
    2. No campo de busca: colar o **Community ID** (ver tabela abaixo)
    3. Clicar no plugin correto (confirmar nome do autor para evitar plugins impostores)
    4. Clicar **Install** (aguardar download)
    5. Após install, clicar **Enable**
    6. Fechar o modal de browse (X ou Esc)
    7. Em `Settings → Community plugins`, confirmar plugin aparece na lista **"Installed plugins"** com toggle ON

    **Lista ordenada (instalar nesta ordem):**

    | # | Community ID para buscar | Autor esperado | Nome visível |
    |---|--------------------------|----------------|--------------|
    | 1 | `templater-obsidian` | SilentVoid13 | Templater |
    | 2 | `periodic-notes` | Liam Cain | Periodic Notes |
    | 3 | `calendar` | Liam Cain | Calendar |
    | 4 | `nldates-obsidian` | argenos | Natural Language Dates |
    | 5 | `obsidian-tasks-plugin` | obsidian-tasks-group | Tasks |
    | 6 | `dataview` | Michael Brenan (blacksmithgu) | Dataview |
    | 7 | `homepage` | mirnovov | Homepage |
    | 8 | `obsidian-tracker` | pyrochlore | Tracker |
    | 9 | `pomodoro-timer` | eatgrass | Pomodoro Timer |
    | 10 | `obsidian-git` | Vinzent | Git |

    **Cuidados específicos:**
    - **Calendar**: há vários plugins com "Calendar" no nome. Garantir que é o de **Liam Cain** (o que integra com Periodic Notes). Outro comum é "Full Calendar" (diferente, mais voltado pra eventos/ics — NÃO é esse).
    - **Tasks**: o correto é **obsidian-tasks-plugin** (grupo maintainer), não "Task Genius" nem "TaskForge".
    - **Git**: autor é **Vinzent03**. Há outro plugin "Obsidian Git" não-oficial que não quer.
    - **Pomodoro Timer**: autor **eatgrass**. Há "Status Bar Pomodoro Timer" e "PomoBar" que são diferentes (sem integração Tasks).

    **Verificação via filesystem (após instalar todos os 10):**
    ```bash
    cd "/e/Claude Code/Agente Pessoal - Obsidian/Vault"
    # Contar plugins habilitados
    cat .obsidian/community-plugins.json
    # Esperado JSON array com exatamente 10 IDs:
    # ["templater-obsidian", "periodic-notes", "calendar", "nldates-obsidian", "obsidian-tasks-plugin", "dataview", "homepage", "obsidian-tracker", "pomodoro-timer", "obsidian-git"]
    #
    # Ou verificar por ls:
    ls .obsidian/plugins/
    # Esperado 10 folders (um por plugin)
    ls .obsidian/plugins/ | wc -l
    # Esperado: 10
    ```
  </how-to-verify>
  <acceptance_criteria>
    - `cat Vault/.obsidian/community-plugins.json` retorna JSON array com exatamente os 10 IDs listados
    - `ls Vault/.obsidian/plugins/` retorna 10 folders (um por plugin)
    - Em `Settings → Community plugins`, todos os 10 plugins listados com toggle **ON** (azul/verde dependendo do tema)
    - Nenhum erro vermelho no canto inferior do Obsidian indicando plugin falhou ao carregar
    - Teste: reabrir Obsidian (fechar e abrir novamente) — os 10 plugins continuam ativos
  </acceptance_criteria>
  <resume-signal>
    Digite "10 plugins instalados e habilitados" quando o JSON tiver 10 IDs. Se algum falhou, lista quais deu problema.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Configurar Templater (FOUND-04) + Homepage + obsidian-git</name>
  <read_first>
    - Vault/_meta/setup.md §3 (configs específicas)
    - Vault/_meta/backup.md regra #2 (obsidian-git settings)
    - Verificar que Plan 02 completou com git init + remote (`cd Vault && git remote -v` deve mostrar origin)
  </read_first>
  <files>
    Vault/.obsidian/plugins/templater-obsidian/data.json
    Vault/.obsidian/plugins/homepage/data.json
    Vault/.obsidian/plugins/obsidian-git/data.json
  </files>
  <what-built>
    Task 2 instalou os 10 plugins. 3 deles precisam de config inicial CRÍTICA antes de Phase 2 (Templater), antes de Phase 3 (Homepage), e para backup imediato (obsidian-git). Os outros 7 usam defaults por enquanto (configs específicas delas ficam em Phase 2/3 onde são usadas).
  </what-built>
  <how-to-verify>
    **Parte A — Templater (FOUND-04):**

    1. `Settings` → `Templater` (na barra lateral esquerda das settings, embaixo em "Community plugins")
    2. **Template folder location:** digitar `Templates` (só o nome do folder, sem barra, sem quotes) — Obsidian deve autocompletar
    3. **Trigger Templater on new file creation:** clicar para ON — **CRÍTICO, sem isso Phase 2 não funciona**
    4. **Automatic jump to cursor:** ON (bom para UX)
    5. **Enable Folder Templates:** ON (vai configurar o mapeamento em Phase 2; só habilitar o feature agora)
    6. **Folder Templates (lista vazia por enquanto):** não adicionar nada aqui; Phase 2 configura

    **Parte B — Homepage:**

    1. `Settings` → `Homepage`
    2. **Homepage:** digitar `00 - Dashboard/Home.md` (arquivo ainda não existe — será criado em Phase 3; OK apontar pra path futuro)
    3. **Open on startup:** ON
    4. **Open in:** selecionar **Reading view** (CRÍTICO — Dataview só renderiza em Reading; sem isso o dashboard fica mostrando source markdown)
    5. **Manage tabs:** selecionar **Replace last note** (começa limpa a cada sessão)
    6. **Commands on load:** deixar vazio por enquanto (pode adicionar "Calendar: Open calendar" em Phase 3)

    **Parte C — obsidian-git (Vinzent03):**

    1. `Settings` → `Git` (ou `Obsidian Git` dependendo da versão)
    2. **Vault backup interval (minutes):** digitar `30`
    3. **Auto backup after stopping file edits (minutes):** deixar `0` (desabilitado) OU `5` se quiser backup mais frequente — começar com 0 e avaliar
    4. **Commit message on auto backup/commit:** deixar default `vault backup: {{date}}` ou ajustar para `vault: {{date}} {{hostname}}`
    5. **Commit when Obsidian is closing:** ON
    6. **Auto pull on startup:** ON (single-device agora, mas blindagem pra futuro)
    7. **Push on backup:** ON (CRÍTICO — sem isso commits ficam locais)
    8. **Custom Git binary path:** deixar vazio (usa git do PATH; se der erro, especificar `C:\Program Files\Git\bin\git.exe`)
    9. **Username and password:** NÃO preencher aqui. Plugin usa as credenciais do git global / SSH key.

    **Teste do obsidian-git:**

    1. Fechar Obsidian
    2. Reabrir → deve tentar `git pull` automático ao abrir (se erro: problema de auth, checar SSH key ou PAT)
    3. Criar um arquivo de teste: em `_meta/`, criar `test-backup.md` com conteúdo "teste obsidian-git"
    4. Aguardar ~1 minuto + salvar
    5. Via palette (Ctrl+P): executar `Obsidian Git: Commit all changes` (commit manual para testar)
    6. Via palette: `Obsidian Git: Push`
    7. Verificar no GitHub (browser): arquivo `_meta/test-backup.md` aparece
    8. **Limpar teste:** deletar `_meta/test-backup.md` → commit+push → GitHub sem o arquivo

    **Verificação via filesystem:**
    ```bash
    cd "/e/Claude Code/Agente Pessoal - Obsidian/Vault"
    cat .obsidian/plugins/templater-obsidian/data.json | grep -i "trigger"
    # Esperado: "trigger_on_file_creation": true

    cat .obsidian/plugins/homepage/data.json | grep -i "home"
    # Esperado: referência a "00 - Dashboard/Home.md"

    cat .obsidian/plugins/obsidian-git/data.json | grep -i "interval"
    # Esperado: "autoSaveInterval": 30 (ou campo equivalente)
    ```
  </how-to-verify>
  <acceptance_criteria>
    - **Templater:** `data.json` contém `"trigger_on_file_creation": true` E `"templates_folder": "Templates"` (ou equivalente dependendo da versão do plugin)
    - **Homepage:** `data.json` contém referência a `"00 - Dashboard/Home.md"` e modo reading
    - **obsidian-git:** `data.json` contém intervalo de auto-backup = 30 (minutos); push on backup = true; commit on close = true
    - **Teste real funcionou:** arquivo criado em `_meta/test-backup.md` foi commited + pushed para GitHub (depois limpo)
    - Push manual via palette não deu erro de autenticação
  </acceptance_criteria>
  <resume-signal>
    Digite "3 plugins configurados + push test OK" quando Templater trigger = ON, Homepage apontando pra Home.md em Reading, obsidian-git pushando com sucesso. Se autenticação falhou na Part C, descrever e debugar antes de continuar.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke test final Phase 1</name>
  <what-built>
    Todas as 4 tasks de Plan 03 + todas de Plans 01 e 02 devem estar completas. Esta é a verificação cross-plan da Phase 1 inteira.
  </what-built>
  <how-to-verify>
    Rodar e reportar resultado de cada verificação:

    **1. Vault skeleton (Plan 01):**
    ```bash
    cd "/e/Claude Code/Agente Pessoal - Obsidian"
    ls Vault/ | wc -l
    # Esperado: 14+ (13 folders + _meta + .gitignore + .obsidian + .git)
    find Vault -maxdepth 2 -name .gitkeep | wc -l
    # Esperado: 13
    ```

    **2. _meta/ documentação (Plan 01):**
    ```bash
    ls Vault/_meta/
    # Esperado: README.md, backup.md, plugins.md, setup.md (4 arquivos)
    ```

    **3. Windows LongPaths (Plan 02):**
    ```powershell
    Get-ItemPropertyValue -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name LongPathsEnabled
    # Esperado: 1
    ```

    **4. Git + GitHub remote privado (Plan 02):**
    ```bash
    cd Vault
    git log --oneline | head -5
    git remote -v
    # Esperado: commits presentes, origin apontando para GitHub
    ```
    Browser: abrir URL do repo em aba anônima → 404 (prova que é private).

    **5. 10 plugins habilitados (Plan 03):**
    ```bash
    cat Vault/.obsidian/community-plugins.json
    # Esperado: JSON array com 10 plugin IDs
    ls Vault/.obsidian/plugins/ | wc -l
    # Esperado: 10
    ```

    **6. Configs críticos (Plan 03):**
    ```bash
    # Templater
    grep -c "trigger_on_file_creation" Vault/.obsidian/plugins/templater-obsidian/data.json
    # Esperado: ≥ 1

    # Homepage
    grep -c "Home" Vault/.obsidian/plugins/homepage/data.json
    # Esperado: ≥ 1

    # obsidian-git
    ls Vault/.obsidian/plugins/obsidian-git/data.json
    # Esperado: arquivo existe
    ```

    **7. Abrir Obsidian + ver os 13 folders + sem erros:**
    - Fechar Obsidian completamente
    - Reabrir com Vault como target
    - Sidebar mostra todas as 13 pastas
    - Bottom-right: nenhum badge vermelho de erro de plugin
    - Command palette (Ctrl+P): digitar "Templater" → aparecem comandos do Templater (prova que está ativo)
    - Command palette: digitar "Dataview" → aparecem comandos do Dataview
    - Command palette: digitar "Obsidian Git" → aparecem comandos do obsidian-git

    **8. Teste end-to-end do backup:**
    - Criar arquivo em `Vault/_meta/phase-1-complete.md` com conteúdo "Fase 1 completa em 2026-04-23"
    - Aguardar 30 minutos OU forçar commit via Ctrl+P → "Obsidian Git: Commit all changes and push"
    - Verificar no GitHub: arquivo aparece em `_meta/phase-1-complete.md` no repo (ainda privado)

    **9. CRÍTICO — privacy check:**
    ```bash
    cd Vault
    # Criar arquivo sensível em _private/
    echo "reflexão pessoal teste - não deve ir pra nuvem" > "_private/privacy-test.md"
    git status
    # Esperado: "nothing to commit" — _private/ ignorado
    # Abrir GitHub → verificar que _private/privacy-test.md NÃO aparece
    rm "_private/privacy-test.md"  # limpar
    ```
  </how-to-verify>
  <acceptance_criteria>
    - Todas as 9 verificações acima retornam output esperado
    - **Phase 1 success criteria do ROADMAP.md confirmados:**
      1. ✅ Usuário abre Obsidian em Vault/ e vê os 11+ folders de topo sem erros (verificação 1, 2, 7)
      2. ✅ Test file com path >250 chars abre/edita/commit sem erro (verificação 3 + teste prático)
      3. ✅ `git push` aparece em GitHub confirmado PRIVADO (verificação 4, 8)
      4. ✅ 10 plugins instalados + Templater dispara em folder mapeado (verificação 5, 6, 7; dispara será testado de verdade em Phase 2 quando houver template)
      5. ✅ `_meta/plugins.md` documenta cada um dos 10 plugins (verificação 2 + conteúdo)
    - **Privacy check passou:** `_private/` ignorado pelo git (verificação 9)
  </acceptance_criteria>
  <resume-signal>
    Digite "Phase 1 COMPLETA" com output das 9 verificações todas OK, ou "Phase 1 — {N} gaps" listando o que falhou para Claude debugar em gap-closure.
  </resume-signal>
</task>

</tasks>

<verification>
Plan 03 completo quando:

1. 10 plugins listados em `community-plugins.json`
2. Templater configurado com trigger ON + folder Templates/
3. Homepage apontando para `00 - Dashboard/Home.md` em Reading mode
4. obsidian-git configurado com auto-backup 30min + push on backup + commit on close
5. Teste real de backup (criar arquivo → commit+push → ver no GitHub privado) funcionou
6. Smoke test cross-plan passou (Task 4)

**Phase 1 COMPLETA** quando todas as 4 tasks deste plano + Plan 01 + Plan 02 passaram.
</verification>

<success_criteria>
Requisitos endereçados por este plano:

- [x] **FOUND-03**: 10 plugins community instalados (Templater, Dataview, Periodic Notes, Calendar, Tasks, Homepage, Tracker, Pomodoro Timer, Natural Language Dates, obsidian-git) — Tasks 1-2
- [x] **FOUND-04**: Templater configurado com "Trigger on new file = ON" e folder `Templates/` — Task 3 Parte A

**Pronto para:** Phase 2 — Templates + Schema + Rubric. Pré-requisitos satisfeitos:
- Templater ativo com folder mapping pronto para ser configurado
- Periodic Notes instalado pra configurar daily/weekly/monthly format
- Pasta `Templates/` existe (de Plan 01) pronta pra receber os templates de Phase 2
- Backup automático rodando → qualquer trabalho em Phase 2 é preservado
</success_criteria>

<output>
Após completion, criar `.planning/phases/01-funda-o/01-03-SUMMARY.md` com:
- Lista dos 10 plugins instalados com versões reais (do Community plugins browser)
- Qualquer plugin que deu problema e como resolveu
- Screenshot opcional do Settings → Community plugins mostrando todos habilitados
- Confirmação dos 3 configs críticos (Templater/Homepage/obsidian-git)
- Output do teste de backup real (criar arquivo → commit+push → GitHub)
- Tempo gasto por Weslley (comparar com estimativa pra calibrar)
- Pattern learned: "obsidian-git auto-detecta .git existing repo" ou qualquer insight
- Próximo passo: se Phase 1 COMPLETA (Task 4 passou todas 9 verificações) → `/gsd-transition` para Phase 2. Se gaps → `/gsd-verify-phase 1` + `/gsd-plan-phase --gaps 1`.
</output>
